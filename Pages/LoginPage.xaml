<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="clientAPP.Pages.LoginPage"
             Title="Вход">

    <ScrollView>
        <StackLayout Padding="30" Spacing="20">

            <!-- Заголовок -->
            <Label Text="Techno-Fix" 
                   FontSize="32" 
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>

            <!-- Иконка -->
            <Label Text="🔧" 
                   FontSize="50"
                   HorizontalOptions="Center"/>

            <!-- Форма входа -->
            <Frame Padding="20" CornerRadius="10" BackgroundColor="#F5F5F5">
                <StackLayout Spacing="15">

                    <!-- Email -->
                    <StackLayout>
                        <Label Text="Email" FontSize="14"/>
                        <Entry Placeholder="Введите email"
                               Text="{Binding Email}"
                               Keyboard="Email"/>
                    </StackLayout>

                    <!-- Пароль -->
                    <StackLayout>
                        <Label Text="Пароль" FontSize="14"/>
                        <Entry Placeholder="Введите пароль"
                               Text="{Binding Password}"
                               IsPassword="True"/>
                    </StackLayout>

                    <!-- Кнопки -->
                    <Button Text="Войти" 
                            Command="{Binding LoginCommand}"
                            BackgroundColor="#2196F3"
                            TextColor="White"
                            CornerRadius="5"/>

                    <Button Text="Регистрация" 
                            Command="{Binding RegisterCommand}"
                            BackgroundColor="#4CAF50"
                            TextColor="White"
                            CornerRadius="5"/>

                    <Button Text="Тестовые данные" 
                            Command="{Binding TestCredentialsCommand}"
                            BackgroundColor="#FF9800"
                            TextColor="White"
                            CornerRadius="5"/>

                    <!-- Индикатор загрузки -->
                    <ActivityIndicator IsRunning="{Binding IsBusy}"
                                       IsVisible="{Binding IsBusy}"
                                       Color="#2196F3"/>

                </StackLayout>
            </Frame>

            <!-- Сообщение об ошибке -->
            <Label Text="{Binding ErrorMessage}"
                   TextColor="#F44336"
                   IsVisible="{Binding HasError}"
                   HorizontalOptions="Center"
                   FontSize="14"/>

            <!-- Тестовые данные -->
            <Frame Padding="15" CornerRadius="10" BackgroundColor="#E8F5E9">
                <StackLayout>
                    <Label Text="Тестовые учетные данные:" 
                           FontSize="12" 
                           TextColor="#388E3C"
                           FontAttributes="Bold"/>
                    <Label Text="Админ: admin@technofix.com / admin123" 
                           FontSize="11" 
                           TextColor="#4CAF50"/>
                    <Label Text="Техник 1: tech1@technofix.com / tech123" 
                           FontSize="11" 
                           TextColor="#4CAF50"/>
                    <Label Text="Техник 2: tech2@technofix.com / tech123" 
                           FontSize="11" 
                           TextColor="#4CAF50"/>
                </StackLayout>
            </Frame>

        </StackLayout>
    </ScrollView>

</ContentPage>