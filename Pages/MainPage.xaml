<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="clientAPP.Pages.MainPage"
             Title="Главная">

    <VerticalStackLayout Spacing="20" Padding="30">

        <!-- Информация о пользователе -->
        <Frame BackgroundColor="{StaticResource Primary}"
               CornerRadius="10"
               Padding="15">
            <VerticalStackLayout Spacing="10">
                <Label Text="Techno-Fix"
                       FontSize="24"
                       FontAttributes="Bold"
                       TextColor="White"/>

                <Label Text="{Binding UserName}"
                       FontSize="18"
                       TextColor="White"/>

                <Label Text="{Binding UserRole, StringFormat='Роль: {0}'}"
                       FontSize="14"
                       TextColor="White"/>
            </VerticalStackLayout>
        </Frame>

        <Button Text="Выйти"
                Command="{Binding LogoutCommand}"
                BackgroundColor="{StaticResource Red}"
                TextColor="White"
                FontSize="16"
                HeightRequest="50"/>

        <!-- Меню -->
        <Label Text="Меню"
               FontSize="20"
               FontAttributes="Bold"
               HorizontalOptions="Center"/>

        <Button Text="👥 Клиенты"
                Command="{Binding NavigateToClientsCommand}"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                FontSize="16"
                HeightRequest="60"
                CornerRadius="10"/>

        <Button Text="💻 Устройства"
                Command="{Binding NavigateToDevicesCommand}"
                BackgroundColor="{StaticResource Secondary}"
                TextColor="White"
                FontSize="16"
                HeightRequest="60"
                CornerRadius="10"/>

    </VerticalStackLayout>

</ContentPage>